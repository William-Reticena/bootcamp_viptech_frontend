import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#00264B",
    },
  },
});

export const styles = {
  appBar: {
    alignItems: "center",
    display: "flex",
    height: "100px",
    justifyContent: "center",
  },
};
