import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#0F4C81",
    },
  },
});

export const styles = {
  boxWrapper: {
    display: "flex",
    justifyContent: "space-between",
    marginTop: "56px",
  },

  typoTitle: {
    fontSize: "2em",
  },
};
