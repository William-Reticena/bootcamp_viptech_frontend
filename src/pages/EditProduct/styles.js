import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#00264B",
    },
    grey: {
      main: "#D9D9D9",
    },
  },
});

export const styles = {
  boxMain: {
    marginTop: "48px",
    width: "50%",
  },

  typoTitle: {
    fontSize: "2em",
  },

  breadcrumbs: {
    marginBottom: "16px",
  },
};
